<%h @genre.name %>

<%= pluralize(@genre.movies.count, 'movie') %></br>
<% if @genre.movies.count > 0 %>
	<% @genre.movies.each do |movie| %>
		<%= link_to movie.title, movie %></br>
	<% end %>
<% end %>
